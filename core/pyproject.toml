[project]
name = "core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "common",
    # enricher/enrich.py calls the AbuseIPDB and GeoLite lookups over HTTP.
    "requests>=2.31",
]

[tool.uv.sources]
common = { path = "../common", editable = true }
