__pycache__/
*.py[cod]
.pytest_cache/
.venv/
# Shared database files — never commit the live DB or its WAL sidecars
data/*.db
data/*.db-shm
data/*.db-wal
# IDE folders
.idea/
.vscode/
# Loaded environment
.env
